<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wiatt.plugin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Manuscript">
        <activity android:name=".myViewPager2.ViewPager2Activity"/>
        <activity android:name=".myListView.ListViewActivity" />
        <activity android:name=".myRecycleView.RecycleViewActivity" />
        <activity android:name=".myFragment.FragmentDemoActivity"/>
        <activity android:name=".MainActivity" />
    </application>

</manifest>